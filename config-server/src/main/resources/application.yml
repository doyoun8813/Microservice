spring:
  application:
    name: config-server
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: file:///Development/99.workspace_study/msa/Microservice/config-server/src/main/resources/config
        encrypt:
          enabled: false
server:
  port: 8080
